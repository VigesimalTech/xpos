name: Build Desktop App

# Builds the X POS desktop installers for trying out: a Windows NSIS installer
# and a macOS (Apple silicon) disk image, attached to the run as artifacts.
# Nothing is published or released. Windows is unsigned (SmartScreen warns
# once). macOS is signed ad hoc, not with a Developer ID: Gatekeeper refuses
# the first open, and System Settings > Privacy & Security > Open Anyway lets
# it run from then on. Unsigned, a downloaded copy is reported as "damaged".
#
# The app version comes from the release tag (v2.9.0-offline.6 -> 2.9.0-offline.6),
# or, for a manual run, 2.9.0-dev.<short sha> so a build names its commit.

on:
  workflow_dispatch: {}
  push:
    tags:
      - "v*-offline.*"
  # Proves a change to this workflow still builds, before it merges.
  pull_request:
    paths:
      - .github/workflows/build-desktop.yml

permissions:
  contents: read

concurrency:
  group: build-desktop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows
            os: windows-latest
            target: --win nsis --x64
            files: frontend/release/*.exe
          - name: macOS
            os: macos-latest
            # build/icon.icns is not in the repo; electron-builder converts the 512 px PNG.
            target: --mac dmg --arm64 --config.mac.icon=build/icon.png
            files: frontend/release/*.dmg

    defaults:
      run:
        shell: bash
        working-directory: frontend

    steps:
      - uses: actions/checkout@v7

      - name: Work out the version
        id: version
        run: |
          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            version="${GITHUB_REF_NAME#v}"
          else
            version="2.9.0-dev.${GITHUB_SHA::7}"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Building X POS $version" >> "$GITHUB_STEP_SUMMARY"

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: yarn
          cache-dependency-path: frontend/yarn.lock

      - name: Install
        run: yarn install --frozen-lockfile

      - name: Build the app bundle
        run: yarn build:electron

      - name: Build the installer
        env:
          # No signing certificate yet (see D4). Windows builds unsigned; the
          # Mac signs ad hoc (mac.identity "-" in electron-builder.yml).
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
          # electron-builder skips Mac signing on pull requests unless told
          # otherwise. Ad hoc signing uses no secrets, so a PR build can sign.
          CSC_FOR_PULL_REQUEST: "true"
        run: >-
          npx electron-builder ${{ matrix.target }} --publish never
          --config.extraMetadata.version=${{ steps.version.outputs.version }}

      - name: Check the Mac signature
        if: matrix.name == 'macOS'
        run: |
          app="$(ls -d release/mac-arm64/*.app)"
          codesign --verify --deep --strict --verbose=2 "$app"
          codesign -dv "$app" 2>&1 | grep -q 'Sealed Resources version=' \
            || { echo "::error::$app is not sealed; Gatekeeper will call it damaged"; exit 1; }

      - uses: actions/upload-artifact@v7
        with:
          name: xpos-${{ steps.version.outputs.version }}-${{ matrix.name }}
          path: ${{ matrix.files }}
          if-no-files-found: error
          retention-days: 30
